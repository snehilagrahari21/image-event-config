import { type ReactNode } from 'react';
import type { RadioSize } from './Radio';
import './RadioGroup.css';
interface RadioGroupContextValue {
    name: string;
    value: string;
    onChange: (value: string) => void;
    isDisabled: boolean;
}
export declare function useRadioGroupContext(): RadioGroupContextValue | null;
export interface RadioGroupProps {
    /** Group label displayed above the radios */
    label?: string;
    /** Name attribute shared by all radios in the group */
    name: string;
    /** Controlled selected value */
    value: string;
    /** Called when selection changes */
    onChange: (value: string) => void;
    /** Size — controls label typography */
    size?: RadioSize;
    /** Disables all radios in the group */
    isDisabled?: boolean;
    /** Radio children */
    children: ReactNode;
    /** Additional class name */
    className?: string;
    /** Orientation of the radio group */
    orientation?: 'Vertical' | 'Horizontal';
}
export declare function RadioGroup({ label, name, value, onChange, size, isDisabled, children, className, orientation, }: RadioGroupProps): import("react/jsx-runtime").JSX.Element;
export declare namespace RadioGroup {
    var displayName: string;
}
export {};
