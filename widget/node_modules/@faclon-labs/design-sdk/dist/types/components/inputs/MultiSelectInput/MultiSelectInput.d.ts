import { type ReactNode, type MouseEvent } from 'react';
import './MultiSelectInput.css';
export interface MultiSelectTag {
    label: string;
    onDismiss?: () => void;
}
export type MultiSelectType = 'multiple' | 'multiple-flex';
export interface MultiSelectInputProps {
    /** Layout type — 'multiple': fixed-height single row with +N overflow; 'multiple-flex': auto-height wrapping tags */
    type?: MultiSelectType;
    /** Label for the field */
    label: string;
    /** Field name for form submissions */
    name?: string;
    /** Placeholder text when no tags and no search value */
    placeholder?: string;
    /** Array of selected items shown as Tag chips */
    tags?: MultiSelectTag[];
    /** Max visible tags before showing "+N more" — only applies to type="multiple" (default 2) */
    maxVisibleTags?: number;
    /** Help text shown below the field */
    helpText?: string;
    /** Error text — shown when validationState is 'error' */
    errorText?: string;
    /** Validation state */
    validationState?: 'none' | 'error';
    /** Whether the field is disabled */
    isDisabled?: boolean;
    /** Whether the dropdown is currently open */
    isOpen?: boolean;
    /** Controlled search/filter value */
    searchValue?: string;
    /** Called when the user types in the search input */
    onSearchChange?: (value: string) => void;
    /** Called when the user presses Backspace on an empty input (to remove last tag) */
    onBackspace?: () => void;
    /** Called when user presses Enter with a non-empty input — for free-form tag creation */
    onSubmit?: (value: string) => void;
    /** Leading icon slot — 16px icon before the value */
    leadingIcon?: ReactNode;
    /** Leading text — prefix text (e.g. currency symbol) */
    leadingText?: string;
    /** Dropdown content — pass DropdownMenu here */
    children?: ReactNode;
    /** Called when the trigger area is clicked */
    onClick?: (e: MouseEvent<HTMLDivElement>) => void;
    /** Called when input receives focus */
    onFocus?: () => void;
    /** Called when input loses focus */
    onBlur?: () => void;
    /** Additional class name */
    className?: string;
}
export declare const MultiSelectInput: import("react").ForwardRefExoticComponent<MultiSelectInputProps & import("react").RefAttributes<HTMLInputElement>>;
