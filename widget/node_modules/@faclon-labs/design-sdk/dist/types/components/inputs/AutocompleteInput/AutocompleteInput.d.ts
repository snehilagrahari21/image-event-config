import { type ReactNode } from 'react';
import { type MultiSelectTag } from '../MultiSelectInput/MultiSelectInput';
import './AutocompleteInput.css';
export type AutocompleteType = 'single' | 'multiple' | 'multiple-flex';
export interface AutocompleteInputProps {
    /** Selection type — 'single': TextInput, 'multiple': fixed-height tags with +N overflow, 'multiple-flex': auto-height wrapping tags */
    type?: AutocompleteType;
    /** Label for the field */
    label: string;
    /** Field name for form submissions */
    name?: string;
    /** Placeholder text */
    placeholder?: string;
    /** Controlled input/search value */
    inputValue?: string;
    /** Called when the user types */
    onInputChange?: (value: string) => void;
    /** Help text shown below the field */
    helpText?: string;
    /** Error text — shown when validationState is 'error' */
    errorText?: string;
    /** Validation state */
    validationState?: 'none' | 'error';
    /** Whether the field is disabled */
    isDisabled?: boolean;
    /** Whether the dropdown is currently open */
    isOpen?: boolean;
    /** Leading icon slot */
    icon?: ReactNode;
    /** Prefix text */
    prefix?: string;
    /** Dropdown content — pass DropdownMenu here */
    children?: ReactNode;
    /** Called when the input receives focus */
    onFocus?: () => void;
    /** Called when the input loses focus */
    onBlur?: () => void;
    /** Additional class name */
    className?: string;
    /** Tags — for type="multiple", array of selected items shown as Tag chips */
    tags?: MultiSelectTag[];
    /** Max visible tags before showing "+N more" — for type="multiple" (default 2) */
    maxVisibleTags?: number;
    /** Called when user presses Backspace on empty input — for type="multiple" */
    onBackspace?: () => void;
    /** Called when user presses Enter with non-empty input — for type="multiple" */
    onSubmit?: (value: string) => void;
}
export declare const AutocompleteInput: import("react").ForwardRefExoticComponent<AutocompleteInputProps & import("react").RefAttributes<HTMLInputElement>>;
