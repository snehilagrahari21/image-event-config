import { type ReactNode, type MouseEvent } from 'react';
import './SelectInput.css';
export interface SelectInputTag {
    label: string;
    onDismiss?: () => void;
}
export interface SelectInputProps {
    /** Label for the select field */
    label: string;
    /** Field name for form submissions */
    name?: string;
    /** Placeholder text when no value is selected */
    placeholder?: string;
    /** Display value — for Single select, the selected option text */
    value?: string;
    /** Tags — for Multiple select, array of selected items shown as Tag chips */
    tags?: SelectInputTag[];
    /** Max visible tags before showing "+N more" (default 2) */
    maxVisibleTags?: number;
    /** Help text shown below the field */
    helpText?: string;
    /** Error text — shown when validationState is 'error' */
    errorText?: string;
    /** Validation state */
    validationState?: 'none' | 'error';
    /** Whether the field is disabled */
    isDisabled?: boolean;
    /** Whether the dropdown is currently open */
    isOpen?: boolean;
    /** Leading icon slot — 16px icon before the value */
    leadingIcon?: ReactNode;
    /** Leading text — prefix text (e.g. currency symbol) */
    leadingText?: string;
    /** Dropdown content — pass DropdownMenu here */
    children?: ReactNode;
    /** Called when the trigger is clicked */
    onClick?: (e: MouseEvent<HTMLButtonElement>) => void;
    /** Additional class name */
    className?: string;
}
export declare const SelectInput: import("react").ForwardRefExoticComponent<SelectInputProps & import("react").RefAttributes<HTMLButtonElement>>;
