import type { ReactNode, HTMLAttributes } from 'react';
import './Tabs.css';
export type TabsVariant = 'Bordered' | 'Borderless' | 'Filled';
export type TabsSize = 'Medium' | 'Large';
export type TabsOrientation = 'Horizontal' | 'Vertical';
export interface TabsProps extends HTMLAttributes<HTMLDivElement> {
    /** Visual variant */
    variant?: TabsVariant;
    /** Tab size — passed down to TabItem children */
    size?: TabsSize;
    /** Orientation — Horizontal (row) or Vertical (column) */
    orientation?: TabsOrientation;
    /** Whether tab items stretch to fill the container width */
    isFullWidth?: boolean;
    /** TabItem children */
    children?: ReactNode;
}
export declare function Tabs({ variant, size, orientation, isFullWidth, children, className, ...props }: TabsProps): import("react/jsx-runtime").JSX.Element;
export declare namespace Tabs {
    var displayName: string;
}
