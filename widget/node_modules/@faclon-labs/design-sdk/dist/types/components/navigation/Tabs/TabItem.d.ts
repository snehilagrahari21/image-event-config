import type { ReactNode, HTMLAttributes } from 'react';
import './TabItem.css';
export type TabItemVariant = 'Bordered' | 'Filled';
export type TabItemSize = 'Medium' | 'Large';
export type TabItemOrientation = 'Horizontal' | 'Vertical';
export interface TabItemProps extends HTMLAttributes<HTMLButtonElement> {
    /** Tab label text */
    label: string;
    /** Visual variant — Bordered: underline/left-line indicator; Filled: bg fill */
    variant?: TabItemVariant;
    /** Size — Medium or Large */
    size?: TabItemSize;
    /** Orientation — controls indicator direction (bottom vs left border) */
    orientation?: TabItemOrientation;
    /** Whether this tab is active */
    isSelected?: boolean;
    /** Whether the tab stretches to fill available space */
    isFullWidth?: boolean;
    /** Whether the tab is disabled */
    isDisabled?: boolean;
    /** Leading item slot — pass TabsLeadingItem */
    leadingItem?: ReactNode;
    /** Trailing item slot — pass TabsTrailingItem */
    trailingItem?: ReactNode;
}
export declare function TabItem({ label, variant, size, orientation, isSelected, isFullWidth, isDisabled, leadingItem, trailingItem, className, ...props }: TabItemProps): import("react/jsx-runtime").JSX.Element;
export declare namespace TabItem {
    var displayName: string;
}
