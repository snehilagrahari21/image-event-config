import type { ReactNode } from 'react';
import './CardBody.css';
export interface CardBodyProps {
    /** Body text (rendered when no children provided) */
    bodyText?: string;
    /** Custom body content slot — takes precedence over bodyText */
    children?: ReactNode;
    /** Additional class name */
    className?: string;
}
export declare function CardBody({ bodyText, children, className, }: CardBodyProps): import("react/jsx-runtime").JSX.Element;
export declare namespace CardBody {
    var displayName: string;
}
