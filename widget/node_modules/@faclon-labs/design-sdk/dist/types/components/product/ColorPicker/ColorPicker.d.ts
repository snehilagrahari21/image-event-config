import { type ReactNode, type HTMLAttributes } from 'react';
import './ColorPicker.css';
export type ColorPickerTab = 'presets' | 'picker';
export interface ColorPickerProps extends HTMLAttributes<HTMLDivElement> {
    /** Which tab is active */
    activeTab?: ColorPickerTab;
    /** Called when tab changes */
    onTabChange?: (tab: ColorPickerTab) => void;
    /** Presets panel content */
    presetsPanel?: ReactNode;
    /** Picker panel content */
    pickerPanel?: ReactNode;
    /** Called when theme toggle is clicked */
    onThemeToggle?: () => void;
    /** Whether dark mode is active (changes toggle icon) */
    isDark?: boolean;
}
export declare function ColorPicker({ activeTab: controlledTab, onTabChange, presetsPanel, pickerPanel, onThemeToggle, isDark, className, ...props }: ColorPickerProps): import("react/jsx-runtime").JSX.Element;
export declare namespace ColorPicker {
    var displayName: string;
}
